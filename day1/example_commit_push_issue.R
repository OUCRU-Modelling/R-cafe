# Create a sequence of numbers
numbers <- c(3, 3, 5, 7, 7, 8) #fix typo error value T to correct value 7 - hungtt

# Calculate the sum of numbers (with a typo in the function name)
mean <- mean(numbers)

# Print the result
print(mean)
